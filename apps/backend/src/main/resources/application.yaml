spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/vue_kotlin_template
    username: yna
    password:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
